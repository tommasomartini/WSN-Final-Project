#ifndef __EXECUTOR_H__   // if x.h hasn't been included yet...
#define __EXECUTOR_H__   //   #define this so the compiler knows it has been included

#include <vector>

using namespace std;

class Executor {
  
};

#endif
